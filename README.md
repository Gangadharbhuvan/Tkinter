# Tkinter
Basics in Tkinter

Requirements
``` 
pip install tkinter
pip install pygame
pip install mutagen
```

For MP3 Player GUI App - First add the songs by choosing option on the top, add the songs, and play :)
